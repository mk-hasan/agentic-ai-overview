> [English](README.md) | **বাংলা**

# Event-Driven উদাহরণ (LangGraph)

ইনবাউন্ড ইভেন্ট/ইমেইল প্রক্রিয়া।

## চালান

```bash
python patterns/15-event-driven/example/main.py --scenario helpdesk --provider deepseek
python patterns/15-event-driven/example/main.py --scenario ecommerce
python patterns/15-event-driven/example/main.py --scenario demand-forecast --no-mlflow
```

সাধারণ CLI: `--provider`, `--scenario`, `--use-mcp`, `--no-mlflow` — [`README.bn.md`](../README.bn.md)।
