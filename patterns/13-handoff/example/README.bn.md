> [English](README.md) | **বাংলা**

# Handoff উদাহরণ (LangGraph)

Tier-1 থেকে Tier-2 হস্তান্তর।

## চালান

```bash
python patterns/13-handoff/example/main.py --scenario helpdesk --provider deepseek
python patterns/13-handoff/example/main.py --scenario ecommerce
python patterns/13-handoff/example/main.py --scenario demand-forecast --no-mlflow
```

সাধারণ CLI: `--provider`, `--scenario`, `--use-mcp`, `--no-mlflow` — [`README.bn.md`](../README.bn.md)।
