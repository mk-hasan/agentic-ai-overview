# 13-handoff — IT Helpdesk

Run this pattern with the **helpdesk** use case.

```bash
# From repo root
python patterns/13-handoff/example/helpdesk/main.py --provider openai
python patterns/13-handoff/example/main.py --scenario helpdesk
```
