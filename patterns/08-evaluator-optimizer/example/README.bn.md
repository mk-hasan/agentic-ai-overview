> [English](README.md) | **বাংলা**

# Evaluator–Optimizer উদাহরণ (LangGraph)

খসড়া → মূল্যায়ন → সংশোধন লুপ।

## চালান

```bash
python patterns/08-evaluator-optimizer/example/main.py --scenario helpdesk --provider deepseek
python patterns/08-evaluator-optimizer/example/main.py --scenario ecommerce
python patterns/08-evaluator-optimizer/example/main.py --scenario demand-forecast --no-mlflow
```

সাধারণ CLI: `--provider`, `--scenario`, `--use-mcp`, `--no-mlflow` — [`README.bn.md`](../README.bn.md)।
