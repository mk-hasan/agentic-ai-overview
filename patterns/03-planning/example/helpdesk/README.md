# 03-planning — IT Helpdesk

Run this pattern with the **helpdesk** use case.

```bash
# From repo root
python patterns/03-planning/example/helpdesk/main.py --provider openai
python patterns/03-planning/example/main.py --scenario helpdesk
```
