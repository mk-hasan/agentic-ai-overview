> [English](README.md) | **বাংলা**

# Routing উদাহরণ (LangGraph)

ইনটেন্ট অনুযায়ী হ্যান্ডলারে রাউট।

## চালান

```bash
python patterns/05-routing/example/main.py --scenario helpdesk --provider deepseek
python patterns/05-routing/example/main.py --scenario ecommerce
python patterns/05-routing/example/main.py --scenario demand-forecast --no-mlflow
```

সাধারণ CLI: `--provider`, `--scenario`, `--use-mcp`, `--no-mlflow` — [`README.bn.md`](../README.bn.md)।
