> [English](README.md) | **বাংলা**

# Parallelization উদাহরণ (LangGraph)

সমান্তরাল টুল/LLM কল, তারপর মার্জ।

## চালান

```bash
python patterns/06-parallelization/example/main.py --scenario helpdesk --provider deepseek
python patterns/06-parallelization/example/main.py --scenario ecommerce
python patterns/06-parallelization/example/main.py --scenario demand-forecast --no-mlflow
```

সাধারণ CLI: `--provider`, `--scenario`, `--use-mcp`, `--no-mlflow` — [`README.bn.md`](../README.bn.md)।
