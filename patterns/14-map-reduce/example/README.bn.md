> [English](README.md) | **বাংলা**

# Map–Reduce উদাহরণ (LangGraph)

ব্যাচ লগ সারসংক্ষেপ।

## চালান

```bash
python patterns/14-map-reduce/example/main.py --scenario helpdesk --provider deepseek
python patterns/14-map-reduce/example/main.py --scenario ecommerce
python patterns/14-map-reduce/example/main.py --scenario demand-forecast --no-mlflow
```

সাধারণ CLI: `--provider`, `--scenario`, `--use-mcp`, `--no-mlflow` — [`README.bn.md`](../README.bn.md)।
