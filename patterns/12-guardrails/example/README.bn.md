> [English](README.md) | **বাংলা**

# Guardrails উদাহরণ (LangGraph)

PII ও নীতি যাচাই।

## চালান

```bash
python patterns/12-guardrails/example/main.py --scenario helpdesk --provider deepseek
python patterns/12-guardrails/example/main.py --scenario ecommerce
python patterns/12-guardrails/example/main.py --scenario demand-forecast --no-mlflow
```

সাধারণ CLI: `--provider`, `--scenario`, `--use-mcp`, `--no-mlflow` — [`README.bn.md`](../README.bn.md)।
