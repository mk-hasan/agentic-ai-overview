# 01-react — IT Helpdesk

Run this pattern with the **helpdesk** use case.

```bash
# From repo root
python patterns/01-react/example/helpdesk/main.py --provider openai
python patterns/01-react/example/main.py --scenario helpdesk
```
