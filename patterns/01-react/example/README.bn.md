> [English](README.md) | **বাংলা**

# ReAct উদাহরণ (LangGraph)

ReAct লুপ: চিন্তা → কাজ → পর্যবেক্ষণ।

## চালান

```bash
python patterns/01-react/example/main.py --scenario helpdesk --provider deepseek
python patterns/01-react/example/main.py --scenario ecommerce
python patterns/01-react/example/main.py --scenario demand-forecast --no-mlflow
```

সাধারণ CLI: `--provider`, `--scenario`, `--use-mcp`, `--no-mlflow` — [`README.bn.md`](../README.bn.md)।
