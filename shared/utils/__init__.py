# Shared utilities for pattern examples.
#
# - env.load_env()           — load repo root .env
# - llm.get_openai_chat_model — OpenAI chat model
# - llm.get_deepseek_chat_model — DeepSeek chat model
# - llm.get_chat_model       — pick provider: openai | deepseek
# - cli.add_provider_argument — --provider flag for example CLIs
